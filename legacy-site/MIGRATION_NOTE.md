# Legacy site snapshot

These files preserve the former mixed landing pages, fragments, and generators at the
sealed legacy source commit. They are comparison evidence, not the active site workflow.

The original private log/state inputs and raw CSV files were intentionally moved to the
sibling Private workspace, so legacy generators may not run here. Use `site/build.py`
for the active Pages portal and `tools/link_private_data.py` only in a trusted sibling
checkout when legacy research scripts need their original CSV paths.
